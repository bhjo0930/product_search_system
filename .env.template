# ===================================
# Environment Variables Template
# ===================================
# Copy this file to .env and fill in your actual values
# Never commit the actual .env file to version control

# ===========================================
# REQUIRED VARIABLES (Must be configured)
# ===========================================

# Google Cloud Configuration
PROJECT_ID="your-google-cloud-project-id"          # Your Google Cloud Project ID
GCS_BUCKET="your-gcs-bucket-name"                  # Cloud Storage bucket for images

# Flask Security Configuration  
SECRET_KEY="your-secure-random-secret-key-here"    # Generate with: python -c "import secrets; print(secrets.token_hex(32))"

# ===========================================
# OPTIONAL VARIABLES (Have default values)
# ===========================================

# Google Cloud Region
LOCATION="asia-northeast3"                         # Default: asia-northeast3

# Firestore Database Configuration
FIRESTORE_DATABASE="firestore"                     # Default: firestore
FIRESTORE_COLLECTION="products"                    # Default: products (batch processor uses "products", others may use "firestore")

# ===========================================
# FEATURE-SPECIFIC VARIABLES (Optional)
# ===========================================

# Google Custom Search API (for web search features)
# Get API key: https://console.developers.google.com/apis/credentials
# Create search engine: https://cse.google.com/
GOOGLE_API_KEY=""                                  # Leave empty to disable web search features
CUSTOM_SEARCH_ENGINE_ID=""                         # Leave empty to disable web search features

# Firecrawl API (for enhanced web scraping in batch processor)
# Sign up: https://www.firecrawl.dev/
FIRECRAWL_API_KEY=""                              # Leave empty to use standard web scraping

# ===========================================
# SYSTEM CONFIGURATION (Optional)
# ===========================================

# Logging Configuration
LOG_LEVEL="INFO"                                   # Options: DEBUG, INFO, WARNING, ERROR

# Processing Configuration (Batch Processor)
MAX_EXTRACTION_TOKENS=8192                        # Maximum tokens for AI extraction
BATCH_SIZE=10                                      # Number of items to process in parallel
MAX_WORKERS=5                                      # Maximum worker threads

# ===========================================
# USAGE IN CODE
# ===========================================
# These variables are used across different modules:
#
# Core modules (embedding_firestore.py, embedding_vertex.py, web_app/app.py):
# - PROJECT_ID (required)
# - LOCATION (optional, defaults to "asia-northeast3") 
# - GCS_BUCKET (required for image storage)
# - SECRET_KEY (required for Flask web app)
# - FIRESTORE_DATABASE (optional, defaults to "firestore")
# - FIRESTORE_COLLECTION (optional, defaults vary by module)
#
# Web app specific (web_app/app.py):
# - GOOGLE_API_KEY (optional, enables Google Custom Search)
# - CUSTOM_SEARCH_ENGINE_ID (optional, required if GOOGLE_API_KEY is set)
#
# Batch processor specific (product_batch_processor/):
# - FIRECRAWL_API_KEY (optional, enables enhanced scraping)
# - MAX_EXTRACTION_TOKENS, BATCH_SIZE, MAX_WORKERS (optional, have defaults)
#
# All modules:
# - LOG_LEVEL (optional, defaults to "INFO")